---
- name: Render nginx index page from template
  ansible.builtin.template:
    src: index.html.j2
    dest: /usr/share/nginx/html/index.html
    owner: nginx
    group: nginx
    mode: "0644"

- name: Ensure nginx is running
  ansible.builtin.systemd:
    name: nginx
    state: started
